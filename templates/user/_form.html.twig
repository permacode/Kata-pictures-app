{{ form_start(form, {'attr': {'class': 'mt-4'}}) }}
    {{ form_widget(form, {'attr': {'class': 'mb-4'}}) }}
    <button class="btn bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
